import 'aframe'

const registerCameraDirector = () => {
  AFRAME.registerComponent('camera-director', {
    /**
     * Initial creation and setting of the mesh.
     */
    init: function () {
    },

  })
}

export default registerCameraDirector()