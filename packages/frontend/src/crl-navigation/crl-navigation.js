import 'aframe'
import registerComponent from '../utils/registerComponent';

const crlNavigation = {
  init: function () {
    const el = this.el
  },
}

export default registerComponent('crl-navigation', crlNavigation)